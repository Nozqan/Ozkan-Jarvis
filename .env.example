# Example environment variables for the project
# Replace these with your actual keys in a .env file
API_KEY=your_api_key_here
GEMINI_API_KEY=your_gemini_api_key_here
# APP_DEBUG=true (Removed to avoid conflict with Android's built-in BuildConfig.DEBUG)
